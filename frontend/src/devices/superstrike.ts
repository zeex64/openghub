import productImage from '../productImage'
import type { CatalogDevice } from './types'

export const superstrikeDevice: CatalogDevice = {
  id: 'superstrike',
  modelIds: ['superstrike'],
  productIds: [],
  name: 'PRO X2 Superstrike',
  family: 'PRO Series',
  availability: 'supported',
  image: productImage,
}

